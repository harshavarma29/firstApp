spring:
  application:
    name: firstApp
  data:
    mongodb:
      host: mongodb.firstapp.svc.cluster.local
      port: 27017
      username: ${MONGO_USER}
      password: ${MONGO_PASSWORD}
      database: firstapp_db
      authentication-database: admin

server:
  port: 7000
